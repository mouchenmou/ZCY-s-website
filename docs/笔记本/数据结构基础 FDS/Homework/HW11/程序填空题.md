## Insertion Sort

The function is to sort `N` elements in non-decreasing order by Insertion Sort.  

以下函数的功能是将 `N` 个元素用插入排序算法按非递减顺序排序。

![](附件/Pasted%20image%2020260526203500.png)

??? success "解答"
    跟PPT上一模一样，理解了insertion sort就是秒杀
    
    - 第一空：`i>0 && A[i-1]>Tmp`
    - 第二空：`A[i]=Tmp`
    