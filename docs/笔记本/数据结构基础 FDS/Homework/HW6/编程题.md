这道题的思想非常重重要，因为它既是一个二叉搜索树，又是一个完全树。

- 首先，给定一个按顺序排列好的数组，它就可以表示这个二叉搜索树的inorder排序方法。
- 其次，这棵树是一棵完全树，所以当这个节点所处的位置是 `i` 的时候，它的左孩子节点是 `2*i` 右孩子是 `2*i+1`。
- 通过完全树的这个性质，可以写一个递归函数来填好Heap这个数组，递归函数的思想就是先放左孩子的数，再放自己的数，最后放右孩子的树。