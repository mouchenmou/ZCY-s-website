!!! abstract
    转置（Transpose）和置换（Permutation）是两种基础的矩阵操作。转置交换行列，置换交换行。当消去需要行交换时，$A = LU$ 就变成了 $PA = LU$。

---

## 1. 转置（Transpose）

转置就是把矩阵的行变成列，列变成行。记作 $A^T$：

$$
A = \begin{bmatrix} 1 & 2 \\ 3 & 4 \\ 5 & 6 \end{bmatrix}, \quad
A^T = \begin{bmatrix} 1 & 3 & 5 \\ 2 & 4 & 6 \end{bmatrix}
$$

$(A^T)_{ij} = A_{ji}$。

**转置的性质：**

- $(A^T)^T = A$
- $(A + B)^T = A^T + B^T$
- **$(AB)^T = B^T A^T$** （顺序反过来！）
- $(A^{-1})^T = (A^T)^{-1}$

**对称矩阵（Symmetric Matrix）：** 如果 $A^T = A$，$A$ 就是对称的。对称矩阵在后面的章节里非常重要（特征值全是实数、特征向量正交）。

!!! tip "一个有用的观察"
    对任意矩阵 $A$（甚至矩形的），$A^T A$ 一定是对称的：
    $$
    (A^T A)^T = A^T (A^T)^T = A^T A
    $$
    这在最小二乘法里会用到。

---

## 2. 置换矩阵（Permutation Matrix）

置换矩阵 $P$ 就是 **单位矩阵打乱了行的顺序** 。左乘 $P$ 会交换矩阵的行。

比如交换两行：

$$
P_{23} = \begin{bmatrix} 1 & 0 & 0 \\ 0 & 0 & 1 \\ 0 & 1 & 0 \end{bmatrix}
$$

$P_{23} A$ 会交换 $A$ 的第 2 和第 3 行。

**置换矩阵的性质：**

- $P$ 的每一行每一列恰好一个 1，其余全 0
- $n \times n$ 的置换矩阵一共有 $n!$ 个
- $P^{-1} = P^T$（置换矩阵的逆就是它的转置）
- 两个置换矩阵的乘积还是置换矩阵

---

## 3. $PA = LU$：带行交换的分解

$A = LU$ 的前提是 **没有行交换** 。如果消去过程中换了行，那分解就变成：

$$
\boxed{PA = LU}
$$

其中 $P$ 是所有行交换的置换矩阵的乘积。$P$ 先作用在 $A$ 上（把行排好），然后消去顺利进行，产生 $LU$。

对于任何可逆方阵，都存在某个 $P$ 使得 $PA = LU$ 成立。这是高斯消去法的一般形式。

---

## 4. 转置与内积

向量的点乘也可以用转置来表达：

$$
v \cdot w = v^T w
$$

---

## 5. 对称置换的例子：Pascal 矩阵

Pascal 矩阵是对称正定的，它的 LU 分解也很对称，$A^{-1}$ 也有漂亮的结构：

![](附件/image-20210107171825654.png)

Pascal 矩阵 $A$ 的 $A^{-1}$ 的元素呈现棋盘格的正负交替模式。

这在后面的章节（投影、最小二乘、正交性）里会频繁出现。本质上， **$Ax = b$ 的第 $i$ 个分量就是 $A$ 的第 $i$ 行（转置后是列）和 $x$ 的点乘** 。
